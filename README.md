# Sit (Babric b1.7.3)
Allows you to sit anywhere!

Adds a `/sit` command and key bind for sitting (`B` by default)

Server & Client mod (Can function without client but the seat height offset will be random if the mod isn't installed clientside)